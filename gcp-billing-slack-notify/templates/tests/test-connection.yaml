apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gcp-billing-slack-notify.fullname" . }}-test-connection"
  labels:
{{ include "gcp-billing-slack-notify.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "gcp-billing-slack-notify.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
